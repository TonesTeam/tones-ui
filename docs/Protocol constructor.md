* Add steps in any place of timeline
* Edit timeline:
	* Edit sequence (move steps)
	* Add step under particular place
	* Delete steps
* Enter step params
* Delete steps
* Settings (pop-up use unused space, include description or not)
* Info (future version)
* Enter name and (if) description